# Roadmap Notes

Use this folder for milestone-specific planning notes that would otherwise make `plan.md` too large.

Suggested split:
- one file per milestone family, for example `m26_runtime.md`
- one file per rollout plan, for example `m27_grammar.md`

Keep `../plan.md` as the master roadmap and link into these files.
